import { Head, router, useForm } from '@inertiajs/react';
import { FormEvent, useState } from 'react';

import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import LibraryLayout from '@/Layouts/LibraryLayout';

interface Course { id: number; year_level: number; course_code: string; course_name: string }
interface Program { id: number; program_code: string; program_name: string; total_years: number; courses: Course[] }

export default function Index({ programs, stats, search }: { programs: Program[]; stats: { programs: number; courses: number }; search?: string }) {
    const [query, setQuery] = useState(search ?? '');
    const program = useForm({ program_code: '', program_name: '', total_years: 4 });
    const course = useForm({ program_id: programs[0]?.id ?? 0, year_level: 1, course_code: '', course_name: '' });
    const filter = (event: FormEvent) => { event.preventDefault(); router.get('/prospectus', { search: query }, { preserveState: true }); };
    const addProgram = (event: FormEvent) => { event.preventDefault(); program.post('/prospectus/store-program', { onSuccess: () => program.reset() }); };
    const addCourse = (event: FormEvent) => { event.preventDefault(); course.post(`/prospectus/${course.data.program_id}/course`, { onSuccess: () => course.reset('course_code', 'course_name') }); };
    return <LibraryLayout><Head title="Prospectus Manager" /><div className="space-y-6"><div><h1 className="text-2xl font-semibold">Prospectus Manager</h1><p className="text-muted-foreground">Maintain Library programs and course discovery data.</p></div><div className="grid gap-3 sm:grid-cols-2"><Card><CardHeader><CardTitle className="text-sm">Programs</CardTitle></CardHeader><CardContent className="text-3xl font-semibold">{stats.programs}</CardContent></Card><Card><CardHeader><CardTitle className="text-sm">Courses</CardTitle></CardHeader><CardContent className="text-3xl font-semibold">{stats.courses}</CardContent></Card></div><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle>Add Program</CardTitle></CardHeader><CardContent><form onSubmit={addProgram} className="space-y-3"><Input placeholder="Program code" value={program.data.program_code} onChange={(event) => program.setData('program_code', event.target.value)} /><Input placeholder="Program name" value={program.data.program_name} onChange={(event) => program.setData('program_name', event.target.value)} /><div className="space-y-2"><Label htmlFor="years">Total years</Label><Input id="years" type="number" min={1} max={6} value={program.data.total_years} onChange={(event) => program.setData('total_years', Number(event.target.value))} /></div><Button disabled={program.processing}>Add program</Button></form></CardContent></Card><Card><CardHeader><CardTitle>Add Course</CardTitle></CardHeader><CardContent><form onSubmit={addCourse} className="space-y-3"><select className="h-10 w-full rounded-lg border px-3 text-sm" value={course.data.program_id} onChange={(event) => course.setData('program_id', Number(event.target.value))}>{programs.map((item) => <option key={item.id} value={item.id}>{item.program_code}</option>)}</select><Input type="number" min={1} max={6} value={course.data.year_level} onChange={(event) => course.setData('year_level', Number(event.target.value))} /><Input placeholder="Course code" value={course.data.course_code} onChange={(event) => course.setData('course_code', event.target.value)} /><Input placeholder="Course name" value={course.data.course_name} onChange={(event) => course.setData('course_name', event.target.value)} /><Button disabled={course.processing || !course.data.program_id}>Add course</Button></form></CardContent></Card></div><Card><CardHeader><CardTitle>Programs and Courses</CardTitle><CardDescription><form onSubmit={filter} className="mt-3 flex gap-2"><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search programs or courses" /><Button>Search</Button></form></CardDescription></CardHeader><CardContent className="space-y-4">{programs.map((item) => <div key={item.id} className="rounded-lg border p-4"><div className="flex items-start justify-between"><div><h2 className="font-semibold">{item.program_code} — {item.program_name}</h2><p className="text-sm text-muted-foreground">{item.total_years} year(s)</p></div><Button variant="destructive" size="sm" onClick={() => confirm(`Delete ${item.program_code}?`) && router.delete(`/prospectus/program/${item.id}`)}>Delete</Button></div><ul className="mt-3 grid gap-2 sm:grid-cols-2">{item.courses.map((entry) => <li key={entry.id} className="rounded bg-muted/40 p-2 text-sm"><strong>{entry.course_code}</strong> — {entry.course_name} <span className="text-muted-foreground">(Year {entry.year_level})</span></li>)}</ul></div>)}</CardContent></Card></div></LibraryLayout>;
}
